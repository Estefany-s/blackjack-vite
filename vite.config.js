// Esto se hizo para que se pudiera desplegar bien en github pages, 

import { defineConfig } from 'vite';

export default defineConfig({
    base: '/blackjack-vite/',
    plugins: []
});

// Luego habia que hacer el build de nuevo 